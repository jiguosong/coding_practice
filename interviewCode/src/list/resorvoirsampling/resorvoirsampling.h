#ifndef resorvoirsampling_H_
#define resorvoirsampling_H_

#include "../Node.h"
#include <vector>
using std::vector;

class resorvoirsampling {
public:
	void dummy();
	int getrandom_reservoir(ListNode<int> *head);
private:

};

#endif
